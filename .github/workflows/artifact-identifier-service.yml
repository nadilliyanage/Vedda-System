name: artifact-identifier-service — CI/CD

on:
  push:
    branches: ["main"]
    paths:
      - "backend/artifact-identifier-service/**"
      - ".github/workflows/artifact-identifier-service.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "backend/artifact-identifier-service/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  security-events: write
  id-token: write

env:
  REGISTRY: ${{ secrets.AZURE_ACR_REGISTRY_URL_ARTIFACT_ID }}
  SERVICE: artifact-identifier-service
  IMAGE_NAME: vedda-system-artifact-identifier-service

jobs:
  audit:
    name: Lint & Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
      - name: Install dependencies
        working-directory: backend/artifact-identifier-service
        run: pip install -r requirements.txt
      - name: Run pip-audit
        working-directory: backend/artifact-identifier-service
        run: |
          pip install pip-audit
          pip-audit -r requirements.txt
        continue-on-error: true

  security-scan:
    name: Container Security Scan
    runs-on: ubuntu-latest
    needs: audit
    steps:
      - uses: actions/checkout@v4
      - name: Build image for scanning
        run: docker build -t ${{ env.IMAGE_NAME }}:scan backend/artifact-identifier-service
      - name: Run Trivy scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "${{ env.IMAGE_NAME }}:scan"
          format: "sarif"
          output: "trivy-results.sarif"
          severity: "CRITICAL,HIGH"
        continue-on-error: true
      - name: Upload Trivy results
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: "trivy-results.sarif"
        continue-on-error: true

  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: [audit, security-scan]
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Login to ACR
        run: |
          echo "${{ secrets.AZURE_ACR_PASSWORD_ARTIFACT_ID }}" | docker login \
            -u "${{ secrets.AZURE_ACR_USERNAME_ARTIFACT_ID }}" \
            --password-stdin "${{ env.REGISTRY }}"
      - name: Build and Push
        run: |
          docker build -t "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" \
                       -t "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}" \
                       backend/artifact-identifier-service
          docker push "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          docker push "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"

  deploy:
    name: Deploy to Azure
    runs-on: ubuntu-latest
    needs: build-and-push
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Update Container App
        uses: azure/CLI@v2
        with:
          inlineScript: |
            az containerapp update \
              --name ${{ env.SERVICE }} \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
              --image ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
              --set-env-vars \
                "BUILD_ID=${{ github.run_id }}" \
                "BUILD_SHA=${{ github.sha }}" \
                "PORT=5009" \
                "METADATA_URL=https://docs.google.com/spreadsheets/d/1i87tZ76DEZoBjL9s086kdN7rgfi1hhUk/edit?usp=drive_link&ouid=115458652219080553373&rtpof=true&sd=true" \
                "FEATURE_EXTRACTOR_URL=https://drive.google.com/file/d/1gmri4MvW7y0bzu3tGAY5EHRGFkPQ9p2Y/view?usp=sharing" \
                "SVM_URL=https://drive.google.com/file/d/1CmbqZHyjz-rpqnghC7IpB4wGysCEiI_P/view?usp=sharing" \
                "SCALER_URL=https://drive.google.com/file/d/1N95DeZyzORjVZ7m9KpX0ewiybmzkRfkv/view?usp=sharing"